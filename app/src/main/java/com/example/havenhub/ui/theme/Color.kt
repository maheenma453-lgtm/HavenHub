package com.example.havenhub.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Primary — Deep Navy Blue (HAVEN text color) ──────────────────
val PrimaryBlue         = Color(0xFF1A3A6B)   // main brand deep blue
val PrimaryBlueLight    = Color(0xFF4A90D9)   // light blue (buildings color)
val PrimaryBlueDark     = Color(0xFF0D2347)   // darker variant (pressed)
val OnPrimaryBlue       = Color(0xFFFFFFFF)   // text/icon on primary

// ─── Gold / Accent (HUB text + waves) ────────────────────────────
val AccentGold          = Color(0xFFC9A84C)   // main gold
val AccentGoldLight     = Color(0xFFE8C96A)   // light gold
val AccentGoldDark      = Color(0xFF9B7D2E)   // dark gold (pressed)
val OnAccentGold        = Color(0xFFFFFFFF)

// ─── Secondary Blue (buildings in logo) ──────────────────────────
val SecondaryBlue       = Color(0xFF4A90D9)
val SecondaryBlueLight  = Color(0xFF82C0FF)
val SecondaryBlueDark   = Color(0xFF1A5FA8)
val OnSecondary         = Color(0xFFFFFFFF)

// ─── Background & Surface ─────────────────────────────────────────
val BackgroundWhite     = Color(0xFFFFFFFF)
val BackgroundLight     = Color(0xFFF5F5F5)   // logo background color
val SurfaceWhite        = Color(0xFFFFFFFF)
val SurfaceVariantLight = Color(0xFFEEF2F8)   // subtle blue-tinted cards

// ─── Dark Theme Surfaces ──────────────────────────────────────────
val BackgroundDark      = Color(0xFF0D1B2A)
val SurfaceDark         = Color(0xFF1A2740)
val SurfaceVariantDark  = Color(0xFF243354)

// ─── Text ─────────────────────────────────────────────────────────
val TextPrimary         = Color(0xFF1A3A6B)   // deep navy — same as brand
val TextSecondary       = Color(0xFF7A8BA6)   // muted blue-gray
val TextDisabled        = Color(0xFFBDBDBD)
val TextPrimaryDark     = Color(0xFFE8EEFF)
val TextSecondaryDark   = Color(0xFF9BA8C0)

// ─── Border & Divider ─────────────────────────────────────────────
val BorderGray          = Color(0xFFDDE3EE)
val BorderGrayDark      = Color(0xFF2E3650)
val DividerLight        = Color(0xFFEEEEEE)
val DividerDark         = Color(0xFF252C3D)

// ─── Semantic ─────────────────────────────────────────────────────
val ErrorRed            = Color(0xFFBA1A1A)
val OnErrorRed          = Color(0xFFFFFFFF)
val ErrorRedDark        = Color(0xFFFFB4AB)
val OnErrorRedDark      = Color(0xFF690005)

val SuccessGreen        = Color(0xFF2E7D32)
val WarningOrange       = Color(0xFFF57F17)

// ─── Gold Gradient helpers ────────────────────────────────────────
val GoldStart           = Color(0xFFE8C96A)   // light gold
val GoldEnd             = Color(0xFF9B7D2E)   // dark gold
































