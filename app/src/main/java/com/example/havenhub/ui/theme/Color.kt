package com.example.havenhub.ui.theme

import androidx.compose.ui.graphics.Color

// ════════════════════════════════════════════════════════════════════
// LIGHT THEME COLORS — unchanged, do not modify
// ════════════════════════════════════════════════════════════════════

// ── Primary Navy ──────────────────────────────────────────────────
val PrimaryNavy          = Color(0xFF1B2B5B)
val PrimaryNavyLight     = Color(0xFF2E4A9E)
val PrimaryNavyDark      = Color(0xFF0F1F45)

// ── Gold Accent ───────────────────────────────────────────────────
val GoldAccent           = Color(0xFFC9A84C)
val GoldAccentLight      = Color(0xFFE8C97A)
val GoldAccentDark       = Color(0xFF9B7D2E)

// ── Secondary Blue ────────────────────────────────────────────────
val SecondaryBlue        = Color(0xFF2E4A9E)
val SecondaryBlueLight   = Color(0xFF5C73C2)
val SecondaryBlueDark    = Color(0xFF1A2F6B)
val OnSecondary          = Color(0xFFFFFFFF)

// ── Backgrounds ───────────────────────────────────────────────────
val BackgroundLight      = Color(0xFFF0F2F5)
val BackgroundDark       = Color(0xFF0D1117)
val BackgroundGray       = Color(0xFFF0F2F5)
val BackgroundWhite      = Color(0xFFFFFFFF)

// ── Surfaces ──────────────────────────────────────────────────────
val SurfaceWhite         = Color(0xFFFFFFFF)
val SurfaceDark          = Color(0xFF161B27)
val SurfaceVariantLight  = Color(0xFFE8EAF0)
val SurfaceVariantDark   = Color(0xFF1E2535)
val CardSurface          = Color(0xFFFFFFFF)

// ── Text ──────────────────────────────────────────────────────────
val TextPrimary          = Color(0xFF1A1A2E)
val TextPrimaryDark      = Color(0xFFE8EAF0)
val TextSecondary        = Color(0xFF888888)
val TextOnPrimary        = Color(0xFFFFFFFF)
val OnAccentGold         = Color(0xFF1A1A2E)

// ── Semantic ──────────────────────────────────────────────────────
val SuccessGreen         = Color(0xFF2ECC71)
val WarningOrange        = Color(0xFFFF6F00)
val AccentOrange         = Color(0xFFFF6F00)
val ErrorRed             = Color(0xFFBA1A1A)
val OnErrorRed           = Color(0xFFFFFFFF)
val ErrorRedDark         = Color(0xFFCF6679)
val OnErrorRedDark       = Color(0xFF1A1A2E)

// ── Legacy aliases ────────────────────────────────────────────────
val PrimaryBlue          = PrimaryNavy
val PrimaryBlueDark      = PrimaryNavyDark
val PrimaryBlueLight     = PrimaryNavyLight
val AccentGold           = GoldAccent
val AccentGoldDark       = GoldAccentDark
val AccentGoldLight      = GoldAccentLight
val OnPrimaryBlue        = Color(0xFFFFFFFF)

// ── Border ────────────────────────────────────────────────────────
val BorderGray           = Color(0xFFDDE1E7)

// ════════════════════════════════════════════════════════════════════
// DARK THEME COLORS — logo-matched navy + gold palette
// ════════════════════════════════════════════════════════════════════

// ── Dark Backgrounds — deep navy layers ──────────────────────────
val DarkBg               = Color(0xFF060D1A)   // deepest background
val DarkBgSecondary      = Color(0xFF0D1B3E)   // card/surface level 1
val DarkBgTertiary       = Color(0xFF1A2A50)   // card/surface level 2
val DarkBgElevated       = Color(0xFF1E3060)   // elevated surfaces

// ── Dark Surfaces ─────────────────────────────────────────────────
val DarkSurface          = Color(0xFF0F1E3D)
val DarkSurfaceVariant   = Color(0xFF162240)
val DarkSurfaceCard      = Color(0xFF112038)

// ── Dark Gold — same as logo gold ────────────────────────────────
val DarkGoldPrimary      = Color(0xFFD4AF37)   // logo gold
val DarkGoldLight        = Color(0xFFF5D060)   // lighter gold for text
val DarkGoldDim          = Color(0xFFB8962E)   // darker gold for subtle use
val DarkGoldFaint        = Color(0xFF1A1608)   // very subtle gold bg tint

// ── Dark Text ─────────────────────────────────────────────────────
val DarkTextPrimary      = Color(0xFFF0F4FF)   // near-white with cool tint
val DarkTextSecondary    = Color(0xFF8899BB)   // muted blue-gray
val DarkTextMuted        = Color(0xFF4A5A7A)   // very muted

// ── Dark Semantic ─────────────────────────────────────────────────
val DarkSuccess          = Color(0xFF3DCC7A)
val DarkWarning          = Color(0xFFFFB347)
val DarkError            = Color(0xFFCF6679)
val DarkOnError          = Color(0xFF1A0A0D)

// ── Dark Border ───────────────────────────────────────────────────
val DarkBorder           = Color(0xFF1E2E50)
val DarkBorderGold       = Color(0xFF2A2010)   // subtle gold-tinted border
