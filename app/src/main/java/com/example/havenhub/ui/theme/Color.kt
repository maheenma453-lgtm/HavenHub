package com.example.havenhub.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Primary Blue ─────────────────────────────────────────────────────────────
val PrimaryBlue         = Color(0xFF1E6BE6)   // main brand blue
val PrimaryBlueLight    = Color(0xFF5E9BFF)   // light variant (dark theme primary)
val PrimaryBlueDark     = Color(0xFF0047B3)   // dark variant (pressed / container)
val OnPrimaryBlue       = Color(0xFFFFFFFF)   // text/icon on primary

// ─── Secondary ───────────────────────────────────────────────────────────────
val SecondaryBlue       = Color(0xFF4A90D9)
val SecondaryBlueLight  = Color(0xFF82C0FF)
val SecondaryBlueDark   = Color(0xFF00539B)
val OnSecondary         = Color(0xFFFFFFFF)

// ─── Accent / Tertiary ────────────────────────────────────────────────────────
val AccentAmber         = Color(0xFFFFB300)   // ratings, highlights, badges
val AccentAmberLight    = Color(0xFFFFE54C)
val AccentAmberDark     = Color(0xFFC68400)
val OnAccentAmber       = Color(0xFF1A1C1E)

// ─── Background & Surface ────────────────────────────────────────────────────
val BackgroundWhite     = Color(0xFFFFFFFF)
val BackgroundLight     = Color(0xFFF4F7FE)   // subtle off-white for screens
val SurfaceWhite        = Color(0xFFFFFFFF)
val SurfaceVariantLight = Color(0xFFE8EFF9)   // cards, input backgrounds

// ─── Dark Theme Surfaces ──────────────────────────────────────────────────────
val BackgroundDark      = Color(0xFF0F1117)
val SurfaceDark         = Color(0xFF1A1E2A)
val SurfaceVariantDark  = Color(0xFF252C3D)

// ─── Text ────────────────────────────────────────────────────────────────────
val TextPrimary         = Color(0xFF0D1B2A)   // main text (light theme)
val TextSecondary       = Color(0xFF8A8A8A)   // hints, subtitles
val TextDisabled        = Color(0xFFBDBDBD)
val TextPrimaryDark     = Color(0xFFE8EEFF)   // main text (dark theme)
val TextSecondaryDark   = Color(0xFF9BA8C0)

// ─── Border & Divider ─────────────────────────────────────────────────────────
val BorderGray          = Color(0xFFE0E0E0)
val BorderGrayDark      = Color(0xFF2E3650)
val DividerLight        = Color(0xFFEEEEEE)
val DividerDark         = Color(0xFF252C3D)

// ─── Semantic ────────────────────────────────────────────────────────────────
val ErrorRed            = Color(0xFFBA1A1A)
val OnErrorRed          = Color(0xFFFFFFFF)
val ErrorRedDark        = Color(0xFFFFB4AB)   // dark theme error
val OnErrorRedDark      = Color(0xFF690005)

val SuccessGreen        = Color(0xFF2E7D32)
val WarningOrange       = Color(0xFFF57F17)
