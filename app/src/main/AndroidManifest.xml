<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ── Permissions ─────────────────────────────────────────── -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- <manifest> tag ke andar, <application> se UPAR -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:name=".HavenApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HavenHub">

        <!-- ── Main Activity ───────────────────────────────────── -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            FCM Push Notification Service
            ─────────────────────────────────────────────────────────
            PATCH APPLIED: Renamed from `.remote.FirebaseMessagingService`
            to `.remote.HavenHubMessagingService`.

            WHY THIS FIX IS NEEDED:
            The old class name `FirebaseMessagingService` clashed with the
            Firebase SDK's own internal parent class
            `com.google.firebase.messaging.FirebaseMessagingService`.
            This caused a silent class-resolution conflict, which stopped
            push notifications from being delivered after the first FCM
            token refresh — because the system could not correctly bind to
            our custom service implementation.

            With this rename, the class name is unique and the Android
            system correctly binds to OUR service, restoring all push
            notification delivery (foreground, background, and killed state).
        -->
        <service
            android:name=".remote.HavenHubMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>